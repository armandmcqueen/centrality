# CPU config that fakes having many CPUs. TODO: This might be redundant with vmagent-gpu.yaml having many CPUs.
vm_id: many-cpus
controlplane_sdk:
  host: controlplane
metrics:
  cpu:
    use_fake: true
    fake:
      num_vals: 64
machine_info:
  use_fake: true
  fake:
    hostname: null
    num_cpus: 64
    cpu_description: null
    host_memory_mb: null
    num_gpus: null