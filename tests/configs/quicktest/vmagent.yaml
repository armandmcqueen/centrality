# Config for quickly testing vmagent. Can be run outside of docker-compose and points to the docker compose
# controlplane on localhost
vm_id: test-machine
metrics:
  cpu:
    use_fake: false
  gpu_utilization:
    use_fake: true
  gpu_memory:
    use_fake: true
  nviida_smi:
    use_fake: true
machine_info:
  use_fake: false
  fake:
    hostname: null
    num_cpus: 8
    cpu_description: Intel(R) Xeon(R) CPU E5-2676 v3 @ 2.40GHz
    host_memory_mb: 16000
    num_gpus: 8
    nvidia_driver_version: 535.129.03
    gpu_memory_mb: 60000
    gpu_type: NVIDIA A100



